import { isAdmin, isAdminFieldLevel } from '@/access/isAdmin'
import { isAdminOrEditor, isAdminOrEditorIfPublished } from '@/access/isAdminOrEditor'
import { isAdminOrSelf } from '@/access/isAdminOrSelf'

export { isAdmin, isAdminFieldLevel, isAdminOrEditor, isAdminOrEditorIfPublished, isAdminOrSelf }
